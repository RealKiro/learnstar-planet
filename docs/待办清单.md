# 学趣星球 · 项目待办清单

> 创建：2026-08-07 · 用途：防止会话上下文重开丢失，记录所有待办与当前状态
> 当前状态：宠物系统 6 阶段改造 + 全量内容优化已完成；宠物文档合并为 `docs/宠物系统全案.md`

## 🎯 当前待办（按顺序）

### 1. 推送远端（需用户交互输入凭据）
- 本地 `main` 有 **9+ 个提交**未 push
- 用户需在输入框执行：`! git push origin main`（HTTPS 需交互输凭据；可先 `! git config --global credential.helper cache`）

### 2. DashboardPage 字段错位 bug + 卡片升级
- **Bug**：前端 `pages/teacher/DashboardPage.vue` 读 `data.top5` / `data.star_student`，但后端 `/api/v1/teacher/dashboard`（TeacherController::dashboard）返回 `top_students`（无 star_student）→ 班级 TOP5/班级之星渲染为空（被 demo 数据掩盖）
- **修法**：对齐字段（后端返回改为 `top5` + `star_student`，或前端映射）
- **顺带升级**：卡片改用新样式（学号右上、奖牌左上、PetSprite 代替 emoji、点 SVG 弹 PetHandbook）——与 OverviewPage/LeaderboardPage 已做的重设计一致
- 涉及文件：`TeacherController.php`（dashboard 返回）、`DashboardPage.vue`、可能 `types`

### 3. serverless-analysis.md 清理
- `docs/../serverless-analysis.md`（仓库根）是 2026-07-10 历史分析快照，内容过期（Laravel 11/PHP 8.3/23 张表/单文件前端 HTML）
- **可选**：更新为当前状态，或标注"已过期/归档"，或删除
- 建议：归档处理（改名或加过期标注），不作为活跃文档

### 4. AI 宠物图片生成（大工程，契约已就绪）
- `宠物系统全案.md` 6.4 节已定义完整契约：**125 物种 × 6 阶段 ≈ 750 张**、目录 `public/pets/{seriesId}/{speciesId}-{stage}.webp`、六阶视觉规范表 + 提示词模板
- **未做**：`utils/petImage.ts` 的 `getPetImageUrl()` + MANIFEST；`PetSprite.vue` 图片优先/SVG 回退分支（`v-if="imgUrl"`）；实际生成图片（AI 生图）；`docs/pet-image-manifest.md` 登记
- 优先级：先重点角色（东方神话 20 / 圣斗士 12 / 宝可梦御三家 / 虹猫蓝兔）~60 张验收风格

## ✅ 已完成（宠物系统）
- 6 阶段进化体系（egg/baby/growing/mature/legendary/transcendent，Lv11-12=归真）：Stage 类型/getLevelStage/stageIndexForLevel/809 等级重映射/computePetArt/Pet.php currentStage/UI 标签徽章/类型/文档
- 全量 119 物种特质 6 阶段（6 性格+6 招式，归真项）
- 诗文/台词 6 槽对齐 + 时序微调（小火龙/杰尼龟修正）
- 六阶 SVG 视觉规范 → AI 提示词模板
- 东方神话 20 角色（封神 10 + 补录 10：孙悟空/八仙/太上老君/钟馗）
- 版权直名政策（非商用致敬，不规避）
- 虹猫蓝兔精简 9、数码宝贝只留真角色 6、星座连接圣斗士 12
- 全案文档合并、家长端引用清理

## 关键记忆
- 详见 `~/.claude/projects/.../memory/pet-system-dongfang-and-stages.md`（宠物系统现状与坑）
- PET_POEMS/PET_EVO_LINES 用 species.name 做 key（改物种名须同步）
- 构建产物 backend/public 已 gitignore（Docker 重建）
