# 学宠星球 · 项目待办清单

> 更新：2026-09-13 ｜ 用途：会话上下文重开后快速恢复项目状态
> **维护约定**：本文件只记「当前未完成事项 + 关键现状」。已完成的历史流水请看 `git log`，不要往这里堆。
> 上一版（至 2026-09-06）曾把已删除的 756 张手绘 SVG 当作待交付物、并把同一份 17 角色清单重复粘贴两遍，已全量重写。

---

## 一、当前状态快照（2026-09-13）

| 项 | 现状 |
|---|---|
| 分支 | `main`，1046 次提交，最新 `d68b982`（2026-09-13 11:52）；**工作区干净**（仅本文件的文档更新待提交） |
| 远端 | `origin` / `github` 两个 remote 指向同一地址 `github.com/RealKiro/learnstar-planet` |
| 后端 | Laravel 12 + PHP 8.5；24 模型 / **31 张表** / 32 迁移 / **约 188 条路由定义**（get 80 · post 77 · put 19 · delete 11 · match 1） |
| 前端 | Vue 3 + Vite 8 + TS 6 + Tailwind 4 + Pinia 3（**唯一在线前端**）。⚠️ **Tailwind 实际未生效**：`vite.config.ts` 只注册了 `@tailwindcss/vite` 插件，但全仓**没有任何 `@import "tailwindcss"`**、模板也未使用工具类 → 它不产出任何样式。真正的样式层是手写 `assets/style.css`（含跨页工具类）+ 各页 `<style scoped>`。若要启用 Tailwind，属独立立项，勿在样式收口时顺手引入。 |
| 其他端 | 微信小程序（10 页）、`mcp-server/server.py`（MCP 机器人） |
| 本机环境 | **无 PHP** → 后端改动无法本地验证，一律走 CI（PHPStan Level 5 + PHPUnit） |

### 宠物内容层（已完成，勿重复劳动）

- **数据层全量覆盖**：`petData(+Extended)` / `petLifeStories` / `petProfiles` / `petTraits` / `stageEmoji` / `seriesStages` / `stageAiPrompt`
- **角色卡 126/126 完整**：`docs/角色卡/{系列}/{speciesId}.md`（10 个系列目录）。每卡含 12 级进化链、六阶段剧情正文、视觉规格（抽检每卡 6 处）、台词诗文、AI 提示词、九维档案
- **互动剧情已织入正文**：提交 `89e956a` 起互动内容写进各阶段「剧情正文」，**不再单列字段** → 在卡里搜不到「互动」二字属正常，不是缺失
- **审计工具**（改动宠物数据后必须重跑）：
  - `frontend-vue/scripts/analyze-pets.mjs` → 数据完整性报告 `docs/pet-audit-report.json`
  - `frontend-vue/scripts/audit-cards.mjs` → 角色卡唯一性 + 契合度核对表
- ⚠️ **手绘 SVG 路线已废弃**：提交 `02659fd`「停用手绘SVG统一程序化艺术，删除756张资源」已删除全部 SVG，`frontend-vue/public/pets/` 目录**不存在**；运行时走程序化艺术 / emoji（`PET_IMAGE_MODE='emoji'`）。
  → 任何提到「756 张 / 432 张 SVG」「M1/M2 重绘」的旧记录一律作废，不要再按那个方向做。

### 技术栈口径（已修正，见 CLAUDE.md）

- **Livewire 已退役并已删除**（2026-09-13）：`backend/app/Livewire/`、`backend/resources/views/` 是 Vue 重构前遗留，**无任何路由可达**（`routes/web.php` 只有 `/health`、`/up`、`/debug` + SPA 兜底）；`layouts/app.blade.php` 曾引用**从未安装**的 FluxUI（`@fluxStyles`），一旦被渲染直接 500。文件已随 P0-1 删除，新功能一律写 Vue。

---

## 二、待办（按优先级）

### P0 · 仓库卫生（2026-09-13 新识别，成本低收益高）

**1. 死代码清理** —— ✅ **已完成并提交（2026-09-13，`749ec31`）**

已删除 7 个无引用文件（`backend/` 跟踪文件 143 → 136）：

| 路径 | 理由 |
|---|---|
| `backend/app/Livewire/Teacher/Dashboard.php`、`ScoreManager.php` | 2 个组件，无路由可达 |
| `backend/resources/views/layouts/app.blade.php`、`livewire/teacher/{dashboard,score-manager}.blade.php` | 3 个视图，无路由渲染；`app.blade.php` 引用未安装的 FluxUI |
| `backend/docker/nginx/default.conf` | 未被任何 Dockerfile 引用 |
| `backend/docker/supervisor/supervisord.conf` | 未被任何 Dockerfile 引用 |

> ⬜ **仍未做**：`composer.json` 里的 `livewire/livewire` 现已成未使用依赖，但**移除它必须同时 `composer update` 刷新 lock**（本机无 PHP）→ 留给 CI / 本地 PHP 环境。不要只手改 `composer.json`。

**2. 工作区根目录残留**（位于 git 仓库**之外**，`C:\Users\ZUOQIHE\Documents\GitHub\learnstar-planet\`）

- `env` —— 7 月旧快照，产品名仍是「学趣星球」，既不是 `.env` 也不是 `.env.example`，纯误导源
- `.pet-audit-tmp/` —— 宠物审计脚本的临时产物（内含 `utils/*.js` 副本）
- `.zcode/plans/` —— 历史计划文档，**建议保留**

**3. 过期文档（需确认处置口径）**

| 文件 | 问题 |
|---|---|
| `docs/pet-audit-report.json` | ✅ 已重跑 `analyze-pets.mjs` 刷新（2026-09-13）。内容与 Sep 6 版**完全一致** → 证明数据层自 Sep 6 未变动；脚本报出的两项待处理见 P3.2 |
| `docs/宠物AI生图提示词.md` | 1.2 MB（Aug 8），已被 `docs/stage-ai-prompts/`（6 画风分文件，Sep 12）取代 |
| `wiki/`（API-Reference / Deployment-Guide / Home） | 与 `docs/api-reference.md`、README 内容重复，需定「以哪份为准」 |

**4. `.env.example` 补默认凭据提示** —— ✅ **已完成（2026-09-13）**

- `ADMIN_PASSWORD` / `BOT_PASSWORD` 两处上方注释均已指向 README「🔐 上线前安全检查」，与 `entrypoint.sh` 的启动自检形成三处呼应
- 上一轮记录「该文件被工具侧安全策略拦截」为**误记**：文件原本就已有 `# ⚠️ …务必修改默认密码` 提示，本轮只是补上 README 指引

### P1 · 前端内联样式清理（✅ **已完成：全站静态内联样式 661 → 0**）

- ✅ **第 1 批（`e3a3078`）**：`assets/style.css` 新增跨页语义工具类（`page-head`/`page-title`/`page-eyebrow`/`section-title`/`card-title`/`field-error`/`req-star`/`icon-close`/`muted-center(-sm)`/`center-pad-20`/`text-muted-13`/`flex-1`/`stack-8`），对 25 个 `.vue` 做 **145 处等价替换**（元素无 class 的 130 处 + `.card/.btn` 上已核对无冲突的 15 处）；顺带消灭 `.flex-1 { flex:1 }` 在 6 个弹窗页各自重复定义的问题
- ✅ **第 2 批 · 6 个重灾区清零**（每页单独提交）：`admin/SchoolSettingsPage`（`a6358d6`）· `admin/AdminShopPage`（`ef22186`）· `admin/AdminScoreRulesPage`（`e24c2b3`）· `teacher/ShopPage`（`861a748`）· `classroom/PKPage`（`b66baa7`）· `teacher/GradesPage`（`cc45fbb`）
- ✅ **第 3 批 · 10 个 `>10` 文件清零**（2026-09-13 续做，每页单独提交）：`layouts/ClassroomLayout` 34→0（`7b2cd24`）· `teacher/AttendancePage` 30→0（`3fac55d`）· `admin/CreateTeacherModal` 28→0（`afbee95`）· `classroom/PokedexPage` 25→0（`2e211de`）· `teacher/BroadcastPage` 24→0（`c1cca67`）· `teacher/SettingsPage` 23→0（`1d70832`）· `teacher/RulesPage` 20→0（`e59663d`）· `admin/GradeUpgradePage` 20→0（`7425898`）· `admin/ImportTeacherModal` 16→0（`08dad8b`）· `admin/AssignClassModal` 15→0（`77f279c`）
- ✅ **第 4 批 · 19 个中小文件清零**：`teacher/ShopCombinedPage` 13→0 · `admin/ResetPasswordModal` 13→0 · `admin/ReportsPage` 13→0 · `admin/DeleteConfirmModal` 10→0 · `auth/LoginPage` 8→0 · `admin/EditTeacherModal` 8→0 · `admin/AdminDebugPage` 7→0 · `teacher/ScoresPage` 6→0 · `admin/TeachersPage` 6→0 · `auth/OAuthCallbackPage` 5→0 · `layouts/TeacherLayout` 5→0 · `teacher/DashboardPage` 4→0 · `admin/DashboardPage` 3→0 · `teacher/PKPage` 2→0 · `admin/AICenterPage` 2→0 · `teacher/ClassroomDisplay` 1→0 · `admin/ClassesPage` 1→0 · `components/pet/PetSprite` 1→0 · `components/common/ToastContainer` 1→0
- **做法**：残留静态内联样式抽为**页内 `<style scoped>` 语义类**（无 style 块则新建），**声明逐字保留**以保证渲染等价；仅余**动态 `:style`** 状态绑定（loading/success/error 三态色等）属有意保留。新类前缀按页取（`vote-`/`bc-`/`imp-`/`asm-`/`rpm-`/`dbg-`…）并逐页 `grep` 查重，避免与既有 scoped 类同名。
- ⚠️ **P1 收尾时发现并修掉的两个坑（已固化进 `vue-inline-style-migrator` 技能）**：
  1. **`vue-tsc` 不是充分验证**：它对**重复属性**（`Duplicate attribute`）不报错，只有 `vite build` 的 vue 模板编译器才报。本次即因此漏出一处误伤——脚本找标签起点时把属性值里的 `<`（`:disabled="…classCode.length < 3"`）误当标签开头，把类名追加成了**第二个 `class` 属性**（`auth/LoginPage`，已修）。**结论：P1 类改动必须跑 `vite build`，不能只跑 `vue-tsc`。**
  2. 标签边界识别必须**引号感知**（`>` 方向：`v-if="a.length > 0"`；`<` 方向同理），否则类名会被塞进属性值中间或生成重复属性。
- ⚠️ **两点遗留（独立待办，勿混进等价重构）**：① 多页存在**硬编码色**（`#f87171`/`#64748B`/`#10B981`/`#F59E0B` 等）**不随主题翻转**，暗色模式下有对比问题；② `admin/SchoolSettingsPage` 的日志级别着色从未生效——模板里 `levelColors[entry.level]` 写在**静态** `style` 属性中（非 `:style`），被 CSS 解析器整条丢弃，且该变量在脚本里**从未定义**。本次仅等价移除，真修需补一张级别→颜色表。
- ⚠️ `classroom/PokedexPage` 的 `<style>` **未加 `scoped`（全局生效）**，属既有设计。本次未改其作用域，新类统一用 `pkdx-` 前缀避免污染；是否改为 scoped 建议单独立项。

### P2 · 架构债 · TeacherController 拆分（**第 0+P1 批已完成**）

- ✅ **第 0+P1 批（`e501b3d`）**：`TeacherController.php` **2707 → 2539 行**；抽出 `TeacherClassScope`（合并 `teacherClassIds`/`getAccessibleClassIds`，含 `isApiBot` 全班级放行）+ `NoticeService`/`BroadcastService`/`AttendanceService`/`GradeService` 共 5 个 Service，19 个控制器方法退化为薄壳，`routes/api.php` 零改动（「抽 Service、不动路由」）
- ✅ 附带修掉一个真 bug（`ebed445`）：`sendBroadcast` 对每个班级连续调用两次 `DisplayEventService::publish`（同样 payload），导致大屏把同一条广播渲染两遍
- 验证（本机便携 PHP 8.5 + PHPStan + `artisan route:list` + `phpunit`）：PHPStan 0 错误、路由 209 条不变、测试 10/10 通过、容器可解析 7 参数构造函数；**重构提交与 bug 修复分成两个提交**，使前者可证明零行为变更
- ⬜ **后续批次**：按 [TeacherController-拆分方案.md](TeacherController-拆分方案.md) 继续抽剩余约 12 个业务域的 Service（每批单独提交 + 同套回归）

### P3 · 宠物内容与报表收尾

**3.1 报表页「宠物进化阶段分布」三处真 bug —— ✅ 已修（2026-09-13）**

⚠️ 上一版清单写「`ReportsPage.vue` 应改用 `PetSprite`」是**误判**：该卡片是**按等级聚合的分布条**（后端只给 `level/count/stage_name`，没有 `speciesId`），物种级精灵图在此不适用。实测真问题有三个：

| # | 问题 | 证据 |
|---|---|---|
| 1 | 每行表情恒为 🥚 | `stageLevel()` 用的是**旧 11 级名表**（星尘/月芽/灵苗…），而新系统 12 级命名是「新生之卵/破壳萌芽/…」，一个都匹配不上 → 全部回退索引 0 |
| 2 | `:key` 重复 | 后端 `petDistribution()` 按 `groupBy('level')` 分组，而 `stage_name` 是 6 大阶段名（新生之卵/幼年/成长期/成熟期/传说级/归真级）→ 多个等级共用同名 |
| 3 | 占比列恒显示 `undefined%` | 后端只返回 `{level, count, stage_name}`，**从不返回 `percentage`** |

修法：`PetDist` 接口改为 `{level, count, stage_name}`；`:key` 改用 `level`；占比改由前端按总数计算；表情改由 `getLevelStage(level)` 查 6 阶段兜底表（口径与后端 `Pet::currentStageEmoji()` 对齐）；删除 `stageLevel()` 并弃用 `@/utils/constants` 的 `getStageEmoji`（已标 `@deprecated`，且绑定旧 11 级系统）。
验证：`vue-tsc -b` 退出码 0 + `vite build` ✓ built in 5.16s。

**3.2 数据审计两项待处理**（`analyze-pets.mjs` 输出）

- `视觉规格缺≥3项: 65` —— 126 物种中 65 个视觉规格缺 3 项以上，需判断是数据缺失还是检查口径问题
- `同阶段雷同 emoji: L1:🥚 = 126` / `L12:🌟 = 14` —— 全部物种 1 级共用 🥚（**疑似设计如此**，蛋期本就同形），L12 有 14 个物种重复
- 其余指标全绿：emoji 重复对 0、档案/诗词/台词/品性/特质缺失全 0、跨物种特质撞车 0、九维与 12 级数据问题 0

### P4 · AI 中心后续（可选）

- 模型多选映射（New API 式 model mapping）
- 供应商配置批量导入导出（CC Switch 式迁移）
- 按天费用趋势（现仅 token 维度）

### 已确认完成（避免重复排查）

- ✅ P2 功能补强：`POST/PUT/DELETE /teacher/students`、`POST /teacher/students/import`、`POST /teacher/pk/challenge`、`GET /teacher/currency/exchange-logs`、`PUT /teacher/notices/{id}/unpublish`、`score-trend` 均已在 `routes/api.php` 注册
- ✅ 宠物九维档案 126×6 全量；`petProfiles` 各阶段差异
- ✅ 班级码前缀化与管理员统一分配；批量重置端点
- ✅ API 机器人账号（`api-bot`，全校班级权限，`BOT_*` 可配 / `BOT_ENABLED=false` 停用）
- ✅ 数据卷持久化修复（卷挂 `/app/storage/*`）、APP_KEY 持久化
- ✅ 导入冲突防护（教师查重 / 学生跨班同学号拦截 / 第三方扫码不重复建号）

---

## 三、环境备忘（本机踩坑）

- Bash 工具 PATH 异常（`ls`/`mkdir`/`dirname` 均 not found），需先执行：
  `export PATH="/c/Users/ZUOQIHE/.workbuddy/binaries/PortableGit/versions/1.2.0/usr/bin:/c/Users/ZUOQIHE/.workbuddy/binaries/PortableGit/versions/1.2.0/bin:$PATH"`
- `git` 实为 Windows 版（`/c/Program Files/Git/cmd/git`），路径参数必须用 `C:/...` 形式，`/c/...` 会报 "No such file or directory"
- 后端一切改动都要在 CI（PHPStan + PHPUnit）里验证，提交信息中注明
- ✅ **前端可在本机自验**（`frontend-vue/node_modules` 已就绪）：
  `node node_modules/vue-tsc/bin/vue-tsc.js -b`（类型检查，含模板表达式）→ `node node_modules/vite/bin/vite.js build`
- ✅ 宠物脚本：`cd frontend-vue && node scripts/analyze-pets.mjs`（审计）/ `audit-cards.mjs`（角色卡核对）
- PowerShell 工具的 stdout 在本机会话中**捕获不到**（命令返回成功但无输出）→ 取证一律用 bash
- 🔴 **禁止对本仓库使用递归删除**（`rm -rf` / `git rm -r <目录>`）：2026-09-13 执行 `git rm -r` 后，`backend/` 整个目录（143 个跟踪文件）从工作区消失，诱因未最终确认。恢复方式：
  `git restore --source=HEAD --staged --worktree -- backend`
  **删除文件请逐文件 `rm -- <file>` + 非递归 `rmdir`，每步后校验文件数。**

---

## 四、本次会话改动（2026-09-13）

**已提交：29 个提交，领先 `89e956a`**（其中 P1 收尾续做 15 个）

### 上轮（14 个）

| 提交 | 内容 |
|---|---|
| `749ec31` | chore：清理 Vue 重构前遗留死代码与过期配置（删 7 文件：Livewire 组件/Blade 视图/nginx/supervisor） |
| `b4c4921` | feat(security)：默认凭据启动自检告警 + 上线前安全检查文档 |
| `8f76f07` | docs：校正技术栈口径与待办清单 + 新增 `TeacherController-拆分方案.md` |
| `3092ce3` | fix(reports)：修正宠物进化阶段分布三个 Bug（表情/key/占比） |
| `e501b3d` | refactor(teacher)：TeacherController 拆分第 0+P1 批（5 个 Service，2707→2539 行） |
| `ebed445` | fix(broadcast)：移除重复的大屏推送调用 |
| `e3a3078` | refactor(frontend)：P1 内联样式收口到全局工具类（145 处 / 25 文件） |
| `a6358d6` | refactor(frontend)：`admin/SchoolSettingsPage` 内联样式清零（52→6） |
| `ef22186` | refactor(frontend)：`admin/AdminShopPage` 内联样式清零（45→7） |
| `e24c2b3` | refactor(frontend)：`admin/AdminScoreRulesPage` 内联样式清零（35→7） |
| `861a748` | refactor(frontend)：`teacher/ShopPage` 内联样式清零（37→6） |
| `b66baa7` | refactor(frontend)：`classroom/PKPage` 内联样式清零（38→3，本页原无 style 块） |
| `cc45fbb` | refactor(frontend)：`teacher/GradesPage` 内联样式清零（34→3，本页原无 style 块） |
| `570bfa3` | docs：更正 `CLAUDE.md`「Flux 从未安装」误述 + 待办清单同步 P1/P2 进度 |

### 本轮（续做 P1 至完成，15 个）

| 提交 | 内容 |
|---|---|
| `7b2cd24` | `layouts/ClassroomLayout` 34→0（投票弹窗 / 三种广播 / 通知 toast 语义类） |
| `3fac55d` | `teacher/AttendancePage` 30→0 |
| `afbee95` | `admin/CreateTeacherModal` 28→0 |
| `2e211de` | `classroom/PokedexPage` 25→0（该页 style 未 scoped，新类用 `pkdx-` 前缀） |
| `c1cca67` | `teacher/BroadcastPage` 24→0 |
| `1d70832` | `teacher/SettingsPage` 23→0（本页原无 style 块） |
| `e59663d` | `teacher/RulesPage` 20→0（本页原无 style 块） |
| `7425898` | `admin/GradeUpgradePage` 20→0（本页原无 style 块） |
| `08dad8b` | `admin/ImportTeacherModal` 16→0 |
| `77f279c` | `admin/AssignClassModal` 15→0 |
| `7a06dd0` | `teacher/ShopCombinedPage` 13→0 |
| `1d0fb4d` | `admin/ResetPasswordModal` 13→0 |
| `9e766c4` | `admin/ReportsPage` 13→0（本页原无 style 块） |
| `a4be52b` | `DeleteConfirmModal` / `LoginPage` / `EditTeacherModal` / `AdminDebugPage`（10+8+8+7→0；含修 `LoginPage` 重复 class 误伤） |
| `d68b982` | 12 个尾量文件清零（`ScoresPage`/`TeachersPage`/`OAuthCallbackPage`/`TeacherLayout`/两个 `DashboardPage`/两个 `PKPage`/`AICenterPage`/`ClassroomDisplay`/`ClassesPage`/`PetSprite`/`ToastContainer`，33 处→0） |

**未提交**

| 文件 | 改动 |
|---|---|
| `docs/待办清单.md` | 本文件（P1 完成状态、两个坑的复盘、遗留项） |

> 后端（PHP）侧两个提交（`e501b3d`/`ebed445`）**尚未经 CI 验证**——本机无系统 PHP，已用便携 PHP 8.5 本地跑通 PHPStan/PHPUnit/路由回归，但仍请关注 GitHub Actions 的 PHPStan Level 5 + PHPUnit 结果。
> 前端侧全部提交均通过 `vue-tsc -b`（0 错误）+ `vite build`；并已用 `scan-dup-class.mjs` 全量确认无「重复 class 属性」误伤。
